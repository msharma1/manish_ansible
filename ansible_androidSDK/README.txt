Usage:
ansible-playbook androidSDK.yml -vvv 2>&1 | tee playbook.log


Roles:

(1) Centos_android-sdk: For installing android SDK on Linux (CentOS)
There is a scope of improvement to make this role indipendent of Linux flavour.
Currently it works only on for CentOS.

